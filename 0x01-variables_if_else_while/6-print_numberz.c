#include <stdio.h>

/**
 * main - prints all single numbers
 * starting from 0
 * Return: Always 0 (Success)
 */

int main(void)
{
	int n;

	for (n = o; n < 10; n++)
	{
		putchar(n);
	}
	putchar('\n');

	return (0);
}
